import styled from '@emotion/styled';

export const HorizontalHeading = styled.div`
  z-index: -1;
  color: #b8b8b8 !important;
  font-weight: 900;
  font-size: 210px;
  font-family: Poppins;
  line-height: 210px;
  mix-blend-mode: overlay;
  overflow: hidden;
  opacity: 0.1;
  position: fixed;
  left: 0px;
  bottom: -40px;
  padding: 0;
  margin: 0;
  pointer-events: none;
`;
